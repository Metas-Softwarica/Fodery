import { StyleSheet } from 'react-native';

export const utilStyles = StyleSheet.create({
  centerXY: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  centerX: {
    alignItems: 'center',
  },
  centerY: {
    justifyContent: 'center',
  },
});
